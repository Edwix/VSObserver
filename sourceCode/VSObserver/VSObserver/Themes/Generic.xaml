<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:VSObserver">

    <Style x:Key="{x:Type local:VSOTextBox}" TargetType="{x:Type local:VSOTextBox}" BasedOn="{StaticResource {x:Type TextBox}}">


            <Setter Property="Margin" Value="2, 2, 2, 0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type local:VSOTextBox}">
                        <!--<Border Background="Transparent" x:Name="borderSelect">
                        <TextBlock VerticalAlignment="Center" HorizontalAlignment="Left" Background="Aqua" Text="{Binding ElementName=NbElement, Path=Text, UpdateSourceTrigger=PropertyChanged}" x:Name="nbElem"/>
                        </Border>-->
                        <Border x:Name="Border"
                            Padding="2"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                            <Grid x:Name="LayoutGrid">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="{Binding RelativeSource={RelativeSource TemplatedParent},
                                                    Path=ActualHeight}" />
                                </Grid.ColumnDefinitions>
                            <ScrollViewer x:Name="PART_ContentHost" Grid.Column="0" />
                            <Border x:Name="PART_SearchIconBorder"
                                  Grid.Column="1"
                                  BorderThickness="1"
                                  Padding="1"
                                  VerticalAlignment="Stretch"
                                  HorizontalAlignment="Stretch"
                                  BorderBrush="Beige"
                                  Background="Aqua">
                                    <TextBlock Text="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=NbElement}"  />
                                </Border>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

    </Style>
</ResourceDictionary>
